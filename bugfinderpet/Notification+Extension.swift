import Foundation

// Notification extension for processed image capture
extension Notification.Name {
    /// Notification posted when a processed image is captured
    static let didCaptureProcessedImage = Notification.Name("didCaptureProcessedImage")
}
